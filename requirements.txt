# Python dependencies for yt-short-clipper-v2 sidecar
# Install with: py -m pip install -r requirements.txt

# AI / LLM
openai>=1.0.0

# YouTube download
yt-dlp>=2026.8.19
# Required by yt-dlp >=2026 for YouTube "impersonation" — curl_cffi lets
# yt-dlp present a real Chrome TLS fingerprint so YouTube cannot fingerprint
# the client as a bot (which causes throttling and extraction hangs).
curl_cffi>=0.7.0

# Video / image processing
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# MediaPipe face tracking
mediapipe>=0.10.0
# MediaPipe Tasks imports matplotlib at module level (drawing_utils)
matplotlib>=3.8.0

# HTTP requests (Whisper API, Repliz API)
requests>=2.31.0

# Optional LOCAL transcription engine (v2.0.88). Runs in-process, so the app
# can transcribe a long local video with no API key, no upload cap and no
# per-request cost. Weights are downloaded on first use, not bundled.
# `onnxruntime` is intentionally NOT required: it is only needed for the VAD
# filter, which we disable, and it would add ~67 MB to the frozen sidecar.
faster-whisper>=1.1.0

# Build tool for freezing the sidecar
pyinstaller>=6.0.0
